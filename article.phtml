<header>
    <nav class="navbar navbar-expand-lg bg-primary" aria-label="Twelfth navbar example">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample10" aria-controls="navbarsExample10" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse justify-content-md-center" id="navbarsExample10">
                <ul class="navbar-nav">
                    <a class="nav-link text-light" aria-current="page" href="index.php">Accueil</a>

                    <a class="nav-link text-light" href="#">Catégories</a>

                    <a class="nav-link text-light" href="#">Profil</a>
                </ul>
            </div>
        </div>
    </nav>
</header>



<article class="all-article">
    <h1 class="all-title-article"><?= $articlesId['title']; ?></h1>
    <p class="category-article"><?= $articlesId['name'] ?></p>
    <?php if ($articlesId['image']) : ?>
        <img src="<?= $articlesId['image'] ?>" alt="" width="500px">
    <?php endif; ?>
    <p class="all-content-article"><?= $articlesId['content']; ?></p>
</article>

<section>
    <h2 style="text-align: center; margin-top: 30px;">Commentaires</h2>
    <form method="POST" action="article.php?id=<?= $idArticle ?>" class="d-flex flex-column align-items-center mt-4">
        <legend style="text-align: center;">Ajouter un commentaire</legend>
        <div class="mb-3 w-50">
            <label for="nickname" class="form-label">Pseudo</label>
            <input type="text" name="nickname" class="form-control" id="nickname" placeholder="Votre pseudo">
            <?php if (isset($errors['nickname'])) : ?>
                <p class="error"><?= $errors['nickname'] ?></p>
            <?php endif; ?>
        </div>
        <div class="mb-3 w-50">
            <label for="content" class="form-label">Commentaire</label>
            <textarea class="form-control" name="content" id="content" rows="5"></textarea>
            <?php if (isset($errors['content'])) : ?>
                <p class="error"><?= $errors['content'] ?></p>
            <?php endif; ?>
        </div>
        <button type="submit" class="btn btn-primary">Envoyer</button>
    </form>

    <?php if (empty($comments)) : ?>
        <p style="text-align: center; margin-top: 30px;">Soyez le premier à ajouter un commentaire !</p>
    <?php else : ?>
        <?php foreach ($comments as $comment) : ?>
            <div class="comment-block">
                <h4 class="title-comment" style="text-align: center;">Ajouté par <?= $comment['nickname']; ?> le <?= (new DateTimeImmutable($comment['createdAt']))->format('d/m/Y') ?> à <?= (new DateTimeImmutable($comment['createdAt']))->format('H:i') ?></h4>
                <p class="comment" style="text-align: center;"><?= $comment['content']; ?></p>
            </div>
        <?php endforeach; ?>
    <?php endif; ?>


</section>

<footer></footer>